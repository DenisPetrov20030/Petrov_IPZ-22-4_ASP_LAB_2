@model HospitalSystem.Models.Appointment

<div class="container py-4">
	<div class="card shadow-sm">
		<div class="card-header bg-warning text-dark">
			<h4><i class="bi bi-pencil"></i> Редагувати запис</h4>
		</div>
		<div class="card-body">
			<form method="post" action="/Appointments/Edit">
				<input type="hidden" name="AppointmentID" value="@Model.AppointmentID" />

				<div class="mb-3">
					<label class="form-label">Ім'я пацієнта <span class="text-danger">*</span></label>
					<input type="text" name="PatientName" class="form-control" value="@Model.PatientName" required>
				</div>

				<div class="mb-3">
					<label class="form-label">Ім'я лікаря <span class="text-danger">*</span></label>
					<input type="text" name="DoctorName" class="form-control" value="@Model.DoctorName" required>
				</div>

				<div class="mb-3">
					<label class="form-label">Лікар (зв'язок)</label>
					<select name="DoctorID" class="form-select">
						<option value="">-- Не вибрано --</option>
						@foreach (var item in ViewBag.Doctors as SelectList)
						{
							<option value="@item.Value" selected="@(item.Value == Model.DoctorID?.ToString())">@item.Text</option>
						}
					</select>
					<small class="form-text text-muted">Оберіть лікаря зі списку для встановлення зв'язку</small>
				</div>

				<div class="mb-3">
					<label class="form-label">Дата і час <span class="text-danger">*</span></label>
					<input type="datetime-local" name="AppointmentDate" class="form-control" 
						   value="@(Model.AppointmentDate != default(DateTime) ? Model.AppointmentDate.ToString("yyyy-MM-ddTHH:mm") : "")" 
						   required>
				</div>

				<div class="mb-3">
					<label class="form-label">Відділення <span class="text-danger">*</span></label>
					<input type="text" name="Department" class="form-control" value="@Model.Department" required>
				</div>

				<div class="mb-3">
					<label class="form-label">Примітки</label>
					<textarea name="Notes" class="form-control" rows="3">@Model.Notes</textarea>
				</div>

				<div class="d-flex justify-content-between">
					<button type="submit" class="btn btn-warning">
						<i class="bi bi-save"></i> Зберегти зміни
					</button>
					<a href="/Appointments" class="btn btn-outline-secondary">
						<i class="bi bi-x-circle"></i> Скасувати
					</a>
				</div>
			</form>
		</div>
	</div>
</div>
